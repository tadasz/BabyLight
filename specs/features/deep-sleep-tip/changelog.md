- 2026-07-11 NO-SPEC — feature folder seeded by /spec-reverse-engineer from existing code
- 2026-07-12 2026-07-10-deep-sleep-tip — Phase 2 (plan group 4): CryDetector (mic cry detection) wired to the engine's cry-anchor path; `sleepTipMicEnabled` setting; bouts logged to `SessionRecord.cryBouts`. Localized ×26; constitution edits applied. On-device QA still owed.
- 2026-07-12 2026-07-10-deep-sleep-tip — Phase 3 (plan group 5): Calibration — per-baby learned offset (bucketed EMA over logged outcomes, ±10 clamp, ≥5-outcome gate, ~20 window) applied at session start + on cry-cessation anchor switch; settings caption + non-destructive "Reset" (`sleepTipLearningResetDate`). Calibration accuracy vs. real nursery data still to be validated.
- 2026-07-12 bugfix/cry-detector-audio-session-crash — **crash fix**: enabling the mic toggle crashed on device. `CryDetector.beginCapture()` read the input format and installed the tap *before* activating the `.record` session, so `AVAudioEngine.start()` raised an uncatchable Obj-C format-mismatch exception. Now activates the session first, then reads the format / installs the tap; also guards against a double `installTap`. (Device-only path — invisible to sim + unit tests.)
