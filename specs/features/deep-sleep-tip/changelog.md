- 2026-07-11 NO-SPEC — feature folder seeded by /spec-reverse-engineer from existing code
- 2026-07-12 2026-07-10-deep-sleep-tip — Phase 2 (plan group 4): CryDetector (mic cry detection) wired to the engine's cry-anchor path; `sleepTipMicEnabled` setting; bouts logged to `SessionRecord.cryBouts`. Localized ×26; constitution edits applied. On-device QA still owed.
- 2026-07-12 2026-07-10-deep-sleep-tip — Phase 3 (plan group 5): Calibration — per-baby learned offset (bucketed EMA over logged outcomes, ±10 clamp, ≥5-outcome gate, ~20 window) applied at session start + on cry-cessation anchor switch; settings caption + non-destructive "Reset" (`sleepTipLearningResetDate`). Calibration accuracy vs. real nursery data still to be validated.
- 2026-07-13 bugfix/cry-detector-audio-session-crash — **crash fix** (confirmed via device crash logs, builds 38 & 39, iPhone 14 Pro / iOS 26.5): enabling the mic toggle crashed at `AVAudioNode.installTapOnBus:…:format:` — `CryDetector.beginCapture()` passed `installTap` an **explicit `format`** that didn't match the input bus's live hardware format, raising an uncatchable Obj-C exception. Fix: install the tap with `format: nil` (engine uses the bus's own format) and build the `SNAudioStreamAnalyzer` lazily from the first buffer's format; also stopped overriding `windowDuration` (out-of-range values can throw), switched to `.default` audio mode, and made `stop()` always remove the tap. (An earlier attempt reordered the session activation — a red herring; the crash was always the tap format. Device-only path: the simulator's fixed input format masked it.)
